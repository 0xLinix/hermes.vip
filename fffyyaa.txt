local Players = game:GetService("Players")
local lp = Players.LocalPlayer

local CREATOR_ID = 4986042426 -- YOUR UserId

local priority = {
    -- [123456789] = true
}

local rank = (lp.UserId == CREATOR_ID) and "creator" or "user"

return {
    rank = rank,
    priority = priority
}
